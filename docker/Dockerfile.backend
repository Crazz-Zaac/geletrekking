FROM node:18-alpine

WORKDIR /usr/src/app

# Copy package.json first
COPY backend/package*.json ./

# Install ALL deps + nodemon (dev)
RUN npm install && npm install -g nodemon

# Copy source code
COPY backend ./

EXPOSE 5000

CMD ["npm", "run", "dev"]
